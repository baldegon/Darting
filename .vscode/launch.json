{
    "version":"0.2.0",
    "configurations": [
        {
            "type": "cppvsdbg",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/game.exe",
            "args": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}